// Nav Bar Definition
export type NavBarItem = {
    id: string;
    href: string;
    text: string;
  };

// Project Definition
  export interface Project {
    title: string,
    shotDesc: string,
    id: string,
}